# 🔄 系统重启方案指南

本文档提供多种重启方案，根据不同场景选择最合适的方法，最大化数据保留。

---

## 📊 数据保留情况对比

| 数据类型 | 存储位置 | 重启后状态 | 说明 |
|---------|---------|-----------|------|
| **持仓数据** | 交易所API | ✅ 100%保留 | 实时从Hyperliquid读取 |
| **账户余额** | 交易所API | ✅ 100%保留 | 实时读取 |
| **止损止盈** | 交易所API | ✅ 100%保留 | 已设置的订单保持不变 |
| **决策日志** | `decision_logs/` | ✅ 100%保留 | 文件系统持久化 |
| **配置信息** | `config.db` | ✅ 100%保留 | 数据库持久化 |
| **持仓时间** | 内存 | ❌ 重置 | 唯一丢失项 |
| **AI调用次数** | 内存 | ❌ 重置 | 仅统计用 |

---

## 方案1️⃣: 优雅停止 + 等待平仓后重启（最安全）

### 适用场景
- ✅ 大规模代码重构
- ✅ 系统架构调整
- ✅ 对数据完整性要求极高

### 操作步骤

```bash
# 1. 访问Web界面查看持仓
打开浏览器: http://localhost:3000

# 2. 等待AI自动平仓（或手动平仓）
# 确保所有持仓都已关闭

# 3. 停止系统
docker-compose down

# 4. 修改代码
# 编辑您需要优化的文件

# 5. 重新构建镜像
docker-compose build

# 6. 启动系统
docker-compose up -d

# 7. 配置交易员（如果是首次启动）
# 访问 http://localhost:3000 配置
```

### 优点
- ✅ **完全无数据丢失**
- ✅ 重启后从零开始，状态清晰
- ✅ 无需担心持仓时间重置问题

### 缺点
- ⚠️ 需要等待持仓平仓（可能几小时）
- ⚠️ 可能错过交易机会

---

## 方案2️⃣: 热更新（不重启容器）⭐ 推荐

### 适用场景
- ✅ **提示词优化** - 修改 `decision/engine.go`
- ✅ **参数调整** - 修改配置参数
- ✅ **非核心逻辑优化** - 大部分Go代码修改
- ✅ **日常迭代优化**

### 操作步骤

```bash
# 1. 修改代码
vim decision/engine.go  # 例如优化提示词

# 2. 重新构建镜像（不停止容器）
docker-compose build nofx

# 3. 滚动更新（零停机时间）
docker-compose up -d --no-deps nofx

# Docker会自动完成以下步骤：
# - 创建新容器
# - 等待新容器健康检查通过
# - 优雅停止旧容器
# - 切换流量到新容器
```

### 验证更新成功

```bash
# 查看容器状态
docker ps | grep nofx

# 查看最新日志
docker logs nofx-trading --tail 50

# 访问Web界面确认
打开浏览器: http://localhost:3000
```

### 优点
- ✅ **停机时间 < 5秒**
- ✅ 持仓数据立即从交易所恢复
- ✅ 决策日志完全保留
- ✅ 适合频繁迭代优化

### 缺点
- ⚠️ 持仓时间会重置（但影响很小，30-60分钟后恢复正常）

---

## 方案3️⃣: 手动记录 + 快速重启（当前使用）

### 适用场景
- ✅ **核心逻辑修改** - 保证金检查、风险验证等
- ✅ **API客户端优化** - 超时设置、重试逻辑等
- ✅ **紧急修复** - 发现bug需要快速修复

### 操作步骤

```bash
# 1. 备份当前持仓状态
cat decision_logs/hyperliquid_admin_deepseek_*/decision_*_cycle*.json | \
  tail -1 | jq '.positions, .account_state' > position_backup_$(date +%Y%m%d_%H%M%S).json

# 2. 修改代码
vim trader/auto_trader.go  # 例如修改保证金检查

# 3. 重新构建镜像
docker-compose build nofx

# 4. 停止容器
docker-compose down

# 5. 启动容器
docker-compose up -d

# 6. 查看启动日志
docker logs nofx-trading --tail 50

# 7. 访问Web界面确认系统正常
打开浏览器: http://localhost:3000
```

### 备份文件位置
- `position_backup_YYYYMMDD_HHMMSS.json` - 持仓快照
- `decision_logs/` - 完整决策历史

### 优点
- ✅ 停机时间 5-10秒
- ✅ 持仓、余额、止损止盈100%保留
- ✅ 有手动备份可参考

### 缺点
- ⚠️ 持仓时间重置（影响评估：轻微）
- ⚠️ AI调用次数重置（仅统计用，无实际影响）

---

## 方案4️⃣: Docker Volume持久化（彻底解决方案）

### 适用场景
- ✅ 生产环境部署
- ✅ 对数据完整性要求极高
- ✅ 希望完全自动化，无需人工干预

### 配置步骤

#### 1. 修改 `docker-compose.yml`

```yaml
services:
  nofx:
    volumes:
      - ./decision_logs:/app/decision_logs      # 已有：决策日志
      - ./coin_pool_cache:/app/coin_pool_cache  # 已有：币种池缓存
      - ./config.db:/app/config.db              # 已有：配置数据库
      - ./trader_states:/app/trader_states      # 新增：运行时状态持久化
```

#### 2. 创建持久化目录

```bash
mkdir -p trader_states
chmod 755 trader_states
```

#### 3. 实现自动保存功能

需要在代码中添加状态持久化逻辑（每分钟自动保存）：
- 持仓首次出现时间
- 系统启动时间
- AI调用次数

### 优点
- ✅ **100%零数据丢失**
- ✅ 自动化，无需人工干预
- ✅ 支持任意时间重启

### 缺点
- ⚠️ 需要修改代码实现持久化逻辑
- ⚠️ 轻微的性能开销（每分钟写一次文件）

---

## 📋 重启场景推荐表

| 修改类型 | 推荐方案 | 数据丢失 | 停机时间 | 操作复杂度 |
|---------|---------|---------|---------|-----------|
| **提示词优化** | 方案2 | 持仓时间 | <5秒 | ⭐ 简单 |
| **配置参数** | Web界面 | 无 | 0秒 | ⭐ 简单 |
| **API优化** | 方案2/3 | 持仓时间 | <10秒 | ⭐⭐ 中等 |
| **核心逻辑** | 方案3 | 持仓时间 | 5-10秒 | ⭐⭐ 中等 |
| **大规模重构** | 方案1 | 无 | 几小时 | ⭐⭐⭐ 复杂 |
| **生产部署** | 方案4 | 无 | <5秒 | ⭐⭐⭐ 复杂 |

---

## 🛡️ 安全检查清单

### 重启前检查 ✓

- [ ] 备份当前持仓状态
- [ ] 确认决策日志完整（`decision_logs/` 目录）
- [ ] 确认配置数据库存在（`config.db`）
- [ ] 记录当前账户余额
- [ ] 记录当前持仓的入场时间（如需要）

### 重启后验证 ✓

- [ ] Docker容器正常运行（`docker ps`）
- [ ] 日志无严重错误（`docker logs nofx-trading`）
- [ ] Web界面可访问（http://localhost:3000）
- [ ] 持仓数据正确显示
- [ ] AI决策周期正常运行
- [ ] 新修改的功能生效

---

## 🚨 紧急回滚

如果重启后发现问题，快速回滚：

```bash
# 1. 停止新版本
docker-compose down

# 2. 切换到之前的镜像
docker images | grep nofx  # 查看所有镜像
docker tag <old-image-id> nofx-nofx:latest

# 3. 重新启动
docker-compose up -d

# 4. 验证系统正常
docker logs nofx-trading --tail 50
```

---

## 📞 常见问题

### Q1: 重启后持仓时间重置，AI会误判吗？

**A:** 影响很小。原因：
- AI主要看价格趋势、技术指标，而非单纯的持仓时间
- 新提示词要求"最少持仓30分钟"，即使重置也会执行
- 30-60分钟后，持仓时间累积到合理值

### Q2: 重启时有持仓会爆仓吗？

**A:** 不会。原因：
- 止损订单已在交易所设置，独立于系统运行
- 重启时间通常<10秒，风险极小
- 系统重启后立即恢复监控

### Q3: 决策日志会丢失吗？

**A:** 不会。原因：
- 决策日志保存在 `decision_logs/` 目录
- 使用Docker Volume挂载，持久化到宿主机
- 即使删除容器，日志仍然保留

### Q4: 如何选择方案？

**A:** 快速决策：
- **日常优化** → 方案2（热更新）
- **紧急修复** → 方案3（快速重启）
- **重大升级** → 方案1（等待平仓）
- **生产环境** → 方案4（持久化）

---

## 📚 相关文档

- [Docker部署文档](./DOCKER_DEPLOY.md)
- [配置说明](./README.zh-CN.md)
- [决策日志分析](./decision_logs/README.md)

---

**最后更新**: 2025-11-03
**版本**: v1.0
**维护者**: AI Trading System Team
